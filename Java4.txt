标识符：代码中所有自定义的名称（如：类名、变量名、方法名）
标识符的命名规则：
  由字母、数字、下划线、$组成
  不能以数字开头
  不能是关键字
  区分大小写

建议：
  根据内容来定义名字，并且使用驼峰命名法
  （驼峰命名法：
    小驼峰（适用于方法、变量）：一个单词时全部小写，多个单词时第一个单词小写后面每个单词首字母大写 "numCount"；
    大驼峰（适用于类命名）：一个单词首字母大写，多个单词时每个单词首字母大写 “AppleCount”）


键盘录入：将键盘输入存为变量
方法：使用Scanner
使用示例：
    Scanner sc = new Scanner(System.in);
        //接收键盘录入的整数
        int num = sc.nextInt();
        //接收见盘录入的小数
        double num2 = sc.nextDouble();
        //接收键盘录入的字符串
        String name = sc.next();
        //输出
        System.out.println("num=" + num);
        System.out.println("num2=" + num2);
        System.out.println("name=" + name);
        
练习：
public class Test {
    public static void main(String[] args) {
        /*定义两个整数num1 num2，从键盘录入数据为两个变量赋值，求两个数的和并打印*/

        // 创建键盘录入数据对象
        Scanner sc = new Scanner(System.in);

        System.out.println("请输入整数1:");
        int num1 = sc.nextInt();
        System.out.println("请输入整数2:");
        int num2 = sc.nextInt();
        System.out.println("两个数的和为：" + (num1 + num2));
    }
}
